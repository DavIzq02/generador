/**
 * Root test package, mirroring the main source tree. Core tests will
 * be able to run fully in isolation, since the Core has no dependency
 * on frameworks or infrastructure.
 */
package generador;
