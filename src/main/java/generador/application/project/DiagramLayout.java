package generador.application.project;

/**
 * The saved visual positions of the elements in a diagram. Independent
 * from the UML structure: it never modifies the PlantUML document.
 *
 * If absent, AutoLayout is used instead when rendering.
 *
 * Structure only: no behavior is implemented at this stage.
 */
public class DiagramLayout {
    // Intentionally left blank. To be defined in a later stage.
}
