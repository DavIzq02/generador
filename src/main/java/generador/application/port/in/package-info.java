/**
 * Input ports (use case boundaries) exposed by the Application layer to
 * the Presentation layer. Each interface expresses one capability required
 * by the system, without any orchestration logic at this stage.
 */
package generador.application.port.in;
