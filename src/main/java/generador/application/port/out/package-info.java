/**
 * Output ports (gateways) that the Application layer depends on and that
 * the Infrastructure layer will implement: persistence, parsing,
 * rendering, layout, export, code smell detection, diagnostics and
 * telemetry. No implementation lives here, only contracts.
 */
package generador.application.port.out;
