/**
 * Use case orchestrators (application services) that will implement the
 * port.in interfaces by coordinating the Core and the port.out gateways.
 *
 * Intentionally empty at this stage: no orchestration logic is
 * implemented yet, only the package placeholder required by Clean
 * Architecture so this layer can be filled in later without restructuring.
 */
package generador.application.service;
