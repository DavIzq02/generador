/**
 * User-facing layer: will invoke the Application layer's input ports
 * (generador.application.port.in) to create/open projects, browse the
 * UML tree, view the diagram, move elements, save/restore layout,
 * export, and see Code Smells, diagnostics and telemetry.
 *
 * The presentation technology (CLI, desktop UI, web UI, etc.) is
 * intentionally not decided yet; this package is prepared but not
 * implemented so it can be filled in later without affecting the
 * Core or the Application layer.
 */
package generador.presentation;
