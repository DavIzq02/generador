/**
 * Core domain concepts that describe the named, addressable building blocks
 * of a UML model as produced by the PlantUML parser: elements, properties,
 * operations, parameters and namespaces.
 *
 * This package is part of the pure Core. It must never depend on
 * frameworks, UI, file system, rendering engines or any external technology.
 */
package generador.core.domain.element;
